Fix dirty UI transition, Checking Package loop, remove In-Lobby

TASK 2 dirty Start UI: _render_phase and _live_dashboard now use
clear_scrollback=True so old prep-phase banner/table never bleeds
through. Launch loop restructured: all packages set to Launching once
before the loop, then render once per completed launch (was 2x per
package). One-shot scrollback clear added at prep-to-supervisor handoff.

TASK 3 Checking Package loops: After each round for-loop, checking_label
is immediately reset to 1/N and force-rendered (guarded by stop_event)
so the user sees 3/3 then 1/3 cycling instead of 3/3 frozen for sleep.

TASK 4 remove In-Lobby: Removed _LEGACY_LOBBY_STATE. __init__ maps
In-Lobby and Lobby initial_status to No Heartbeat. _STATE_DISPLAY_MAP
maps Lobby to No Heartbeat. _HEALTHY_STATES keeps STATUS_LOBBY for
_PackageWorker backward compat only; WatchdogSupervisor never produces it.

TASK 5 layout skeleton: already implemented; logs, border detection,
2-col grid for <=6 packages, portrait/>6 skeletons confirmed passing.

Probe: p-a1927430ee  screen=1280x720  packages=3  rounds=9
